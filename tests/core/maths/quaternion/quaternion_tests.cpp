#include <gtest/gtest.h>

#include "core/maths/public/Quaternion.h"

using namespace PrismaEngine;

TEST(QuaternionTest, Identity)
{
	EXPECT_TRUE(false);
}

TEST(QuaternionTest, FullInitialization)
{
	EXPECT_TRUE(false);
}

TEST(QuaternionTest, AxisRotationInitialization)
{
	EXPECT_TRUE(false);
}

TEST(QuaternionTest, IsNormalized)
{
	EXPECT_TRUE(false);
}

TEST(QuaternionTest, Normalize)
{
	EXPECT_TRUE(false);
}

TEST(QuaternionTest, Length)
{
	EXPECT_TRUE(false);
}

TEST(QuaternionTest, AddIdentity)
{
	EXPECT_TRUE(false);
}

TEST(QuaternionTest, Add)
{
	EXPECT_TRUE(false);
}

TEST(QuaternionTest, AdditionCommutativity)
{
	EXPECT_TRUE(false);
}

TEST(QuaternionTest, AdditionAssociativity)
{
	EXPECT_TRUE(false);
}

TEST(QuaternionTest, SubtractIdentity)
{
	EXPECT_TRUE(false);
}

TEST(QuaternionTest, Subtract)
{
	EXPECT_TRUE(false);
}

TEST(QuaternionTest, MultiplyIdentity)
{
	EXPECT_TRUE(false);
}

TEST(QuaternionTest, Multiply)
{
	EXPECT_TRUE(false);
}

TEST(QuaternionTest, MultiplicationNonCommutativity)
{
	EXPECT_TRUE(false);
}

TEST(QuaternionTest, MultiplicationAssociativity)
{
	EXPECT_TRUE(false);
}

TEST(QuaternionTest, MultiplicationDistributivity)
{
	EXPECT_TRUE(false);
}

TEST(QuaternionTest, Scale)
{
	EXPECT_TRUE(false);
}

TEST(QuaternionTest, Inverse)
{
	EXPECT_TRUE(false);
}

TEST(QuaternionTest, FromEuler)
{
	EXPECT_TRUE(false);
}

TEST(QuaternionTest, ToEuler)
{
	EXPECT_TRUE(false);
}

TEST(QuaternionTest, GetRotatedRight)
{
	EXPECT_TRUE(false);
}

TEST(QuaternionTest, GetRotatedUp)
{
	EXPECT_TRUE(false);
}

TEST(QuaternionTest, GetRotatedForward)
{
	EXPECT_TRUE(false);
}

TEST(QuaternionTest, ToMatrix)
{
	EXPECT_TRUE(false);
}